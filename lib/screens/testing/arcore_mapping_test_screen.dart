/*
 * ARCore Mapping Proof-of-Concept (Abandoned)
 * This file is no longer used.
 */
import 'package:flutter/material.dart';

class ARCoreMappingTestScreen extends StatelessWidget {
  const ARCoreMappingTestScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Abandoned')));
  }
}
